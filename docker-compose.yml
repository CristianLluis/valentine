services:
  web:
    image: node:22-bookworm-slim
    container_name: valentine
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - ./:/app
      - ./node_modules:/app/node_modules
    command: sh -c "corepack pnpm install && corepack pnpm run dev"
    restart: unless-stopped
